apiVersion: v1
kind: Service
metadata:
  name: express-frontend-service
spec:
  selector:
    app: express-frontend-service
  ports:
  - name: express-frontend-service
    port: 3000
    targetPort: 3000
  type: NodePort